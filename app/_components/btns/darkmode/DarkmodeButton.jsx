import Button from "@components/btns/(default)/Button.jsx";
import React  from 'react';

export default function DarkmodeButton() {
	return (
		<Button style={{
			width : 40,
			height: 40,
		}}>
			버튼
		</Button>
	);
}
